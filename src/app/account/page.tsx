import { Metadata } from "next"

export const metadata:Metadata={
  title:"Account"
}
export default function page() {
  return (
    <div>

        <h1>
        account
        </h1>
    </div>
  )
}
